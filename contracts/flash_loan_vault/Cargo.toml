[package]
name = "flash_loan_vault"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk.workspace = true
emergency_guard = { path = "../emergency_guard" }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
